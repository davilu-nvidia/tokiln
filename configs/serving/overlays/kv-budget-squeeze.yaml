# Experiment-only: squeeze the GPU KV budget to make L2/L3 gains observable
# (with 141GB HBM an oversized L1 masks tiered-cache benefits; see design doc appendix A.1).
workers_patch: {mem_fraction: 0.55}
