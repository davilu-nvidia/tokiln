# M2 step one: host DRAM offload only.
kv:
  l2:
    enabled: true
    host_mem_gb: 512            # leave headroom vs inventory.dram_gb; validated at render
    write_policy: write_through
    page_size: 64
