# 声明式节点清单 —— preflight / render / deploy 的输入。
cluster:
  name: tokiln-dev
nodes:
  - name: node1
    host: 10.6.131.9           # H20-GPU-09
    ssh: {user: root, port: 22}
    roles: [control, worker]
    gpus: {sku: H20-3e-141GB, count: 8, hbm_gb: 141}
    dram_gb: 2015              # free -g 实测
    nvme:  {path: /raid, free_gb_min: 2000}
    rdma:  {devices: [mlx5_0, mlx5_1, mlx5_2], link: roce}   # ibv_devinfo: PORT_ACTIVE
  - name: node2
    host: 10.6.131.10          # H20-GPU-10 — 已声明未启用 (今日实验只用 node1)
    enabled: false
    ssh: {user: root, port: 22}
    roles: [worker]
    gpus: {sku: H20-3e-141GB, count: 8, hbm_gb: 141}
    dram_gb: 2015
    nvme:  {path: /raid, free_gb_min: 2000}
    rdma:  {devices: [mlx5_0], link: roce}
