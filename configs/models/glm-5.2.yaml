model:
  name: glm-5.2
  source: ""                    # TODO: hf://... 或内部镜像 URI (license 确认后填)
  revision: ""                  # pin commit
  quant: fp8                    # M0 spike 确认 fp8 checkpoint 可得性; 否则 bf16
  context_len: 131072
  served_model_name: glm52      # 与 ta-repro 现状对齐: glm52 / glm52ta 由 router 决定
  chat_template: glm-5.2
  reasoning_parser: glm45       # spike 01 验证实际 parser 名
  tool_call_parser: glm45
  weights_cache: /raid/model_hub
